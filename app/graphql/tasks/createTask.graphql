mutation createTask(
  $projectId: Float!
  $completeDate: String!
  $taskName: String!
  $creator: Float!
  $members: String!
) {
  createTask(
    projectId: $projectId
    completeDate: $completeDate
    taskName: $taskName
    creator: $creator
    members: $members
  )
}
