mutation createProject(
  $members: String!
  $projectName: String!
  $projectLead: Float!
) {
  createProject(
    members: $members
    projectName: $projectName
    projectLead: $projectLead
  )
}
